# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION EXAMPLE
# =============================================================================
# Bu faylni .env.production deb nomlang va barcha qiymatlarni to'ldiring!
# Keyin serverga copy-paste qiling

# -----------------------------------------------------------------------------
# Telegram Bot Configuration
# -----------------------------------------------------------------------------
# BotFather dan olingan bot tokenini kiriting
# Misol: BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
BOT_USERNAME=your_bot_username

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------
# Docker Compose ishlatganda host: postgres (service name)
# DIQQAT: Kuchli parol ishlating!
POSTGRES_USER=azizbot
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD_123!@#
POSTGRES_DB=aziz_bot_db
DB_PORT=5432

# Prisma uchun DATABASE_URL (Docker ichida)
# Host nomi: postgres (docker-compose service name)
DATABASE_URL=postgresql://azizbot:CHANGE_THIS_TO_STRONG_PASSWORD_123!@#@postgres:5432/aziz_bot_db?schema=public&connect_timeout=30&pool_timeout=30&socket_timeout=30

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=3000

# Log Level: error, warn, info, debug
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Web Panel Configuration
# -----------------------------------------------------------------------------
# Server IP manzilingizni qo'ying
# Misol: http://161.35.172.239:3000/admin/
WEB_PANEL_URL=http://YOUR_SERVER_IP:3000/admin/

# -----------------------------------------------------------------------------
# Payment Configuration (PayMe) - Optional
# -----------------------------------------------------------------------------
# PayMe to'lov integratsiyasi (kerak bo'lsa to'ldiring)
PAYME_MERCHANT_ID=
PAYME_KEY=
PAYME_TEST_KEY=
PAYME_ENDPOINT=https://checkout.paycom.uz

# -----------------------------------------------------------------------------
# Default Channel Configuration - Optional
# -----------------------------------------------------------------------------
# Agar default kanal qo'shmoqchi bo'lsangiz, commentni olib tashlang:
# DEFAULT_DATABASE_CHANNEL_LINK=@your_channel
# DEFAULT_DATABASE_CHANNEL_NAME=My Channel

# -----------------------------------------------------------------------------
# QADAMLAR:
# -----------------------------------------------------------------------------
# 1. Bu faylni .env.production deb nomlang
# 2. BOT_TOKEN ni BotFather dan oling va kiriting
# 3. POSTGRES_PASSWORD ni kuchli parolga o'zgartiring
# 4. DATABASE_URL dagi parolni ham o'zgartiring (POSTGRES_PASSWORD bilan bir xil)
# 5. WEB_PANEL_URL ga server IP manzilingizni qo'ying
# 6. Faylni serverga copy-paste qiling yoki scp orqali yuklang
# 7. Serverda: docker compose up -d
